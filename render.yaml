services:
  - type: web
    name: appeals-tracker
    runtime: docker
    plan: free
    healthCheckPath: /up
    envVars:
      - key: RAILS_ENV
        value: production
      - key: RAILS_LOG_TO_STDOUT
        value: "true"
      - key: RAILS_MASTER_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: appeals_tracker
          property: connectionString

  - type: worker
    name: appeals-tracker-worker
    runtime: docker
    envVars:
      - key: RAILS_ENV
        value: production
      - key: RAILS_LOG_TO_STDOUT
        value: "true"
      - key: RAILS_MASTER_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: appeals_tracker
          property: connectionString
      - key: SOLID_QUEUE_WORKER
        value: "true"

databases:
  - name: appeals_tracker
    plan: free
